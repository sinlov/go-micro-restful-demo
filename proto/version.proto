syntax = "proto3";

package com.sinlov.demo.micro.restful;

service Version {
    rpc Version(Request) returns (Response) {}
}

message Request {
    string name = 1;
}

message Response {
    string msg = 1;
}